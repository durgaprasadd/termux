set ruler
set number
imap cl<tab> console.log();<left><left>
imap fu<tab> function(){<left><left>
imap c<tab> const 
imap l<tab> let 
imap le<tab> length
imap f<tab> for(){<left><left>
imap w<tab> while(){<left><left>
imap i<tab> if(){<left><left>
imap r<tab> return ;<left>
